const IconStyle = {
  fontSize: "24px",
  padding: "4px",
  cursor: "pointer",
  color: "#030301",
};

const ClearIcon = () => {
  return <i style={IconStyle} className="bi bi-x-circle" />;
};

export default ClearIcon;
