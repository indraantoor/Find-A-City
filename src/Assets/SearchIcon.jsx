const IconStyle = {
  fontSize: "24px",
  padding: "4px",
  cursor: "pointer",
  color: "#030301",
};

const SearchIcon = () => {
  return <i style={IconStyle} className="bi bi-search" />;
};

export default SearchIcon;
