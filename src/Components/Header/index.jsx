import { HeaderContainer, LogoText } from "./style";

const Header = () => {
  return (
    <HeaderContainer>
      <LogoText>FindCity.com</LogoText>
    </HeaderContainer>
  );
};

export default Header;
